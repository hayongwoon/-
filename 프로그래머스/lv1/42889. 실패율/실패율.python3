def solution(N, stages):
    answer = []
    staging_cnt = 0
    d = {}
    for i in range(1, N + 1):
        if stages.count(i):
            yet_clear = stages.count(i)
            staging_players = len(stages) - staging_cnt
            staging_cnt += yet_clear
    
            d[i] = yet_clear / staging_players
        else:
            d[i] = 0

    d = sorted(d.items(), key=lambda x: x[1], reverse=True)
    for j in d:
        answer.append(j[0])

    return answer